---
title: 时间轴
sidebar_position: 1
---

import Link from '@docusaurus/Link';
import timelineData from '../cust-plugins/timeline-data.json';

export const Timeline = () => {
  // --- 分组逻辑 ---
  const groupedData = timelineData.reduce((acc, item) => {
    const year = item.date.split('.')[0];
    if (!acc[year]) acc[year] = [];
    acc[year].push(item);
    return acc;
  }, {});

  const years = Object.keys(groupedData).sort((a, b) => b - a);

  return (
    <div style={{ marginTop: '2rem' }}>
      {years.map((year) => (
        <div key={year} className="margin-bottom--xl">
          {/* 年份大标题 */}
          <h2 style={{ 
            display: 'flex', 
            alignItems: 'center', 
            fontSize: '2rem',
            color: 'var(--ifm-color-primary)',
            borderBottom: 'none' 
          }}>
            {year}
            <span style={{ 
              flex: 1, 
              height: '2px', 
              background: 'var(--ifm-color-emphasis-200)', 
              marginLeft: '20px' 
            }}></span>
          </h2>

          <section style={{
            borderLeft: '3px solid var(--ifm-color-emphasis-300)',
            paddingLeft: '30px',
            marginLeft: '15px',
            position: 'relative'
          }}>
            {groupedData[year].map((item, idx) => (
              <div key={idx} style={{ marginBottom: '30px', position: 'relative' }}>
                {/* 时间轴实心小圆点 */}
                <div style={{
                  position: 'absolute',
                  left: '-37px', // 微调位置使其居中对齐线条
                  top: '10px',
                  width: '12px',
                  height: '12px',
                  borderRadius: '50%',
                  // 关键修改：背景设为主色，边框设为背景色或不设，实现实心效果
                  backgroundColor: 'var(--ifm-color-primary)',
                  border: '2px solid var(--ifm-background-color)',
                  boxShadow: '0 0 0 1px var(--ifm-color-primary)', // 可选：增加一圈极细的外发光感
                }} />

                {/* 1. 标题在上 */}
                <h3 style={{ margin: '0', border: 'none', lineHeight: '1.4' }}>
                  <Link to={item.link} style={{ textDecoration: 'none' }}>
                    {item.title}
                  </Link>
                </h3>

                {/* 2. 日期在下 */}
                <small style={{ 
                  color: 'var(--ifm-color-emphasis-600)', 
                  fontWeight: 'bold', 
                  display: 'block',
                  marginTop: '4px' 
                }}>
                  {item.date.split('.').slice(1).join('-')}
                </small>
              </div>
            ))}
          </section>
        </div>
      ))}

      {years.length === 0 && (
        <p>暂无记录。</p>
      )}
    </div>
  );
};

<Timeline />
